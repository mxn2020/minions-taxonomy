[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-taxonomy"
version = "0.2.0"
description = "Organizational metadata — groups, categories, tags, namespaces, statuses, and priorities"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["taxonomy","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-taxonomy"
Repository = "https://github.com/mxn2020/minions-taxonomy"

[tool.hatch.build.targets.wheel]
packages = ["minions_taxonomy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
